which [-a] command
根据 PATH 环境变量所规范的路径查询可执行文件路径

-a 列出全部路径,而非第一个

列出 ls 命令的路径
# which ls

无法列出 cd 命令的路径,因为 cd 命令是bash内建命令,而不是由 PATH 规范的.
# which cd

更多定位命令:
which   根据 PATH 环境变量所规范的路径查询可执行文件路径.
locate  查找文件.基于文件数据库.
whereis 查找特定类型的文件.基于文件数据库.
